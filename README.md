# Alfred Zed Remote

Alfred workflow for opening Zed SSH remote projects.

- `!zo` lists and filters remote projects from Zed `ssh_connections`.
- `!za ssh user@example.com /home/user/project` adds a remote project to Zed settings.

Selecting a `!zo` result runs `zed ssh://user@example.com/home/user/project`.
